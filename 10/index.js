let var1 = 4;

//Output
//The following output displays the addition of a number to a variable and the result is 8
console.log("Addition : ".concat(var1 + 4));

/** 
 * This is multiline comments and what id does is that it extends the comments to multiple line.
 * like this.
 * and this
 * and so on. So if we have to write something lengthy in the comments then we usually use this type of comments
*/
console.log("Subtraction : ".concat(12 - var1));
console.log("Multiplication : ".concat(var1 * 2));
console.log("Division : ".concat(32 / var1));
